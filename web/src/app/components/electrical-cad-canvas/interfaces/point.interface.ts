/**
 * Represents a point in 2D space
 * Used for grid points and spatial calculations
 */
export interface Point {
  /** X coordinate of the point */
  x: number;
  /** Y coordinate of the point */
  y: number;
}
